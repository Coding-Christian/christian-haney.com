import React from 'react';
import ReactDOM from 'react-dom';
import CSSTransitionGroup from 'react-transition-group';

const placeHolder = (<h1>This is the personal webpage for web developer Christian Haney. It is a work in progress.</h1>);

ReactDOM.render(placeHolder, document.querySelector('#root'));
